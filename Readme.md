import re 
from collections import defaultdict
failed = defaultdict(int) 
success = defaultdict(int) 
timeline = defaultdict(list) # ip → list of timestamps 
hourly = defaultdict(int) # hour → attack count 
failed_pattern = r'Failed password for .+ from (\d+\.\d+\.\d+\.\d+)' 
success_pattern = r'Accepted password for .+ from (\d+\.\d+\.\d+\.\d+)' 
time_pattern = r'(\w+\s+\d+\s+\d+:\d+):\d+' 
with open('example.txt', 'r') as file: 
         for line in file: line = line.strip() # Check failed logins 
              ch = re.search(failed_pattern, line) 
              if ch:
                       ip = ch.group(1) 
                       failed[ip] += 1 # Extract hour for timeline 
                       t = re.search(time_pattern, line)
                        if t: 
                             hour = t.group(1) 
                             timeline[ip].append(hour)
                             hourly[hour] += 1 # Check successful logins 
             ch = re.search(success_pattern, line) 
             if ch: 
                    success[ch.group(1)] += 1 # Write report to file
 with open('report.txt', 'w') as report: 
          report.write("=== SSH Brute Force Report ===\n\n")
          for ip, count in sorted(failed.items(), key=lambda x: -x[1]):
                      severity = "HIGH" if count >= 10 else "MEDIUM"
                      if ip in success: 
                            line_out = f"[CRITICAL] {ip} — {count} fails + SUCCESSFUL LOGIN ⚠️" 
                     else: 
                           line_out = f"[{severity}] {ip} — {count} failed attempts" 
                           print(line_out) 
                           report.write(line_out + "\n") # Show attack timeline for this IP 
                     if timeline[ip]: 
                           times = ", ".join(timeline[ip]) 
                           print(f" Timeline: {times}") 
                           report.write(f" Timeline: {times}\n") # Peak attack hour 
         if hourly: 
                      peak = max(hourly, key=hourly.get) 
                      summary = f"\nPeak attack hour: {peak} ({hourly[peak]} attempts)" 

def write_html_report(failed, success, timeline, hourly):
    peak = max(hourly, key=hourly.get) if hourly else "N/A"
    
    rows = ""
    for ip, count in sorted(failed.items(), key=lambda x: -x[1]):
        severity = "CRITICAL" if ip in success else ("HIGH" if count >= 10 else "MEDIUM")
        color = {"CRITICAL": "#ff4444", "HIGH": "#ff8800", "MEDIUM": "#ffcc00"}[severity]
        times = ", ".join(timeline[ip][:3])  # show first 3 timestamps
        rows += f"""
        <tr>
            <td>{ip}</td>
            <td style="color:{color};font-weight:bold">{severity}</td>
            <td>{count}</td>
            <td>{"⚠️ YES" if ip in success else "No"}</td>
            <td>{times}</td>
        </tr>"""

    html = f"""<!DOCTYPE html>
<html>
<head>
    <title>SSH Brute Force Report</title>
    <style>
        body {{ font-family: Arial, sans-serif; padding: 2rem; background: #0d1117; color: #e6edf3; }}
        h1 {{ color: #58a6ff; }}
        table {{ width: 100%; border-collapse: collapse; margin-top: 1rem; }}
        th {{ background: #161b22; padding: 10px; text-align: left; color: #58a6ff; }}
        td {{ padding: 10px; border-bottom: 1px solid #21262d; }}
        .peak {{ background: #161b22; padding: 1rem; border-radius: 8px; margin-top: 1rem; }}
    </style>
</head>
<body>
    <h1>🔐 SSH Brute Force Detection Report</h1>
    <p>Generated by: Dan's Log Analyzer v1.0</p>
    <table>
        <tr>
            <th>IP Address</th>
            <th>Severity</th>
            <th>Failed Attempts</th>
            <th>Successful Login</th>
            <th>First Seen</th>
        </tr>
        {rows}
    </table>
    <div class="peak">
        📊 Peak attack hour: <strong>{peak}</strong> ({hourly.get(peak, 0)} attempts)
    </div>
</body>
</html>"""

    with open("report.html", "w") as f:
        f.write(html)
    print("Report saved → report.html")


                      print(summary) report.write(summary + "\n") 
                      print("\nReport saved to report.txt")
